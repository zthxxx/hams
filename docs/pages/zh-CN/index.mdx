import Link from 'next/link'

<div className="landing-container">
  <div className="landing-hero">
    <h1 className="landing-title">hams 🐹</h1>
    <div className="landing-tagline">声明式工作站环境管理工具</div>
    <div className="landing-description">
      像仓鼠囤积食物一样，hams 将你的环境配置 —— 包、设置、dotfiles、git 仓库 ——
      囤积到 YAML 文件中，在新机器上一条命令全部恢复。
    </div>
    <div className="landing-actions">
      <Link href="/zh-CN/docs" className="landing-btn-primary">
        开始使用 →
      </Link>
      <Link href="https://github.com/zthxxx/hams" className="landing-btn-secondary" target="_blank">
        GitHub
      </Link>
    </div>
  </div>

  <div className="landing-features">
    <div className="landing-feature">
      <h3>CLI 优先，自动记录</h3>
      <div><code>hams brew install git</code> 安装的同时自动记录，无需手动编辑配置。</div>
    </div>
    <div className="landing-feature">
      <h3>一条命令恢复一切</h3>
      <div><code>hams apply --from-repo=user/store</code> 在新机器上恢复所有环境。</div>
    </div>
    <div className="landing-feature">
      <h3>15 个内置 Provider</h3>
      <div>Homebrew、apt、pnpm、npm、cargo、go、VS Code、git、macOS defaults 等。</div>
    </div>
    <div className="landing-feature">
      <h3>基于 Diff 的更新</h3>
      <div>手动编辑 YAML，运行 <code>hams apply</code>，自动对比状态并执行变更。</div>
    </div>
  </div>

  <div className="landing-example">
    <h2>使用 hams 的日常</h2>

```bash
# 安装工具并自动记录
hams brew install htop

# 三个月后，在新电脑上
hams apply --from-repo=zthxxx/hams-store

# 查看管理的包
hams list
```

  </div>
</div>
