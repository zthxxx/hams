# Tasks — 2026-04-16-defer-tui-and-notify

Status: the deferral notes have already been merged into the main
`tui-logging` spec — both TUI alternate-screen rendering and the
notification system are correctly documented as v1.1-deferred at
`openspec/specs/tui-logging/spec.md` lines 3–7, 40–42, and 65–67.
No further spec edits needed.

- [x] v1-status notes on the alternate-screen + popup + notification
      requirements — present in `tui-logging/spec.md`. Each marks the
      requirement as "Deferred to v1.1" and explains the shipped
      behavior (plain log lines, no notifications).
- [x] `task check` passes — verified 2026-04-17, 32/32 packages PASS.
- [ ] Archive this change folder at release time (deferred).
