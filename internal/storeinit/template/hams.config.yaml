# hams store project-level config (auto-generated by `hams` on first run)
# Machine-scoped fields (tag, profile_tag, machine_id) MUST NOT appear here.
# Set those via `hams config set <key> <value>` in the global config instead
# (see ~/.config/hams/hams.config.yaml).
#
# This file is a placeholder so the store is a valid hams project. You can
# safely add project-wide settings (e.g. `provider_priority`) below.
