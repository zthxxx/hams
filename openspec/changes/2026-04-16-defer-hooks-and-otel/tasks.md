# Tasks — 2026-04-16-defer-hooks-and-otel

- [ ] Write spec delta for `schema-design` marking `hooks:` block as deferred (v1 silently ignores; v1.1 wires up).
- [ ] Write spec delta for `cli-architecture` marking OTel CLI-integration as deferred.
- [ ] Apply manual spec edits to `openspec/specs/schema-design/spec.md` and `openspec/specs/cli-architecture/spec.md` (same pattern as the lucky-defer commit `f4c0f20`).
- [ ] Verify `task check` passes.
- [ ] Commit atomically.
