import Link from 'next/link'
import { Pre } from 'nextra/components'

<div className="landing-container">
  <div className="landing-hero">
    <h1 className="landing-title">hams 🐹</h1>
    <div className="landing-tagline">Declarative IaC for your workstation</div>
    <div className="landing-description">
      Like a hamster hoarding supplies, hams hoards your environment configurations —
      packages, settings, dotfiles, git repos — for one-command restoration on any new machine.
    </div>
    <div className="landing-actions">
      <Link href="/en/docs/why-hams" className="landing-btn-primary">
        Get Started →
      </Link>
      <Link href="https://github.com/zthxxx/hams" className="landing-btn-secondary" target="_blank">
        GitHub
      </Link>
    </div>
  </div>

  <div className="landing-features">
    <div className="landing-feature">
      <h3>CLI-first, auto-record</h3>
      <div><code>hams brew install git</code> installs AND records. No hand-editing config first.</div>
    </div>
    <div className="landing-feature">
      <h3>One-command restore</h3>
      <div><code>hams apply --from-repo=user/store</code> on a fresh machine restores everything.</div>
    </div>
    <div className="landing-feature">
      <h3>15 builtin providers</h3>
      <div>Homebrew, apt, pnpm, npm, cargo, go, VS Code, git, macOS defaults, and more.</div>
    </div>
    <div className="landing-feature">
      <h3>Diff-based updates</h3>
      <div>Hand-edit YAML, run <code>hams apply</code>, and it diffs against state to reconcile.</div>
    </div>
  </div>

  <div className="landing-example">
    <h2>A typical day with hams</h2>

    <div className="landing-example-code">
      ```bash
      # Install a tool and auto-record it
      hams brew install htop

      # Three months later, on a new laptop
      hams apply --from-repo=zthxxx/hams-store

      # See what's managed
      hams list
      ```
    </div>
  </div>
</div>
